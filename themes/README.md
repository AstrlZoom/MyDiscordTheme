# Themes

`AstrlGlass Theme.theme.css` is the editable source theme file.

Use it for AstrlGlass metadata, color variables, spacing, roundness, transparency, and background settings.

For manual installation, run `pnpm build` and use `dist/AstrlGlass Theme.theme.css`.
